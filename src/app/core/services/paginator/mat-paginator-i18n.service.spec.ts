/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { MatPaginatorI18nService } from './mat-paginator-i18n.service';

describe('Service: MatPaginatorI18n', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [MatPaginatorI18nService]
    });
  });

  it('should ...', inject([MatPaginatorI18nService], (service: MatPaginatorI18nService) => {
    expect(service).toBeTruthy();
  }));
});
